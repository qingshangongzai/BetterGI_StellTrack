# 设计语言规范

## 1. 基础元素

### 1.1 颜色系统

|颜色类型 |色值 |描述 |
|---|---|---|
|背景色 |#ffffff |应用背景色，纯白色 |
|卡片背景色 |#ffffff |卡片组件背景色 |
|主色 |#66ccff |主要交互颜色 |
|主色悬停 |#66ccff |主色悬停状态 |
|主色按下 |#3399ff |主色按下状态 |
|次要色 |#ffffff |次要按钮背景色 |
|次要色悬停 |#f5f5f5 |次要按钮悬停状态 |
|次要色按下 |#e5e5e5 |次要按钮按下状态 |
|文本色 |#323130 |主要文本颜色 |
|次要文本色 |#666666 |次要文本颜色 |
|成功色 |#107c10 |成功状态颜色 |
|错误色 |#d13438 |错误状态颜色 |
|警告色 |#ff8c00 |警告状态颜色 |
|边框色 |#d0d0d0 |主要边框颜色 |
|浅色边框 |#e0e0e0 |次要边框颜色 |
|网格线 |#e8e8e8 |表格网格线颜色 |

```css
:root {
  --color-bg: #ffffff;
  --color-card-bg: #ffffff;
  --color-primary: #66ccff;
  --color-primary-hover: #66ccff;
  --color-primary-pressed: #3399ff;
  --color-secondary: #ffffff;
  --color-secondary-hover: #f5f5f5;
  --color-secondary-pressed: #e5e5e5;
  --color-text: #323130;
  --color-text-secondary: #666666;
  --color-success: #107c10;
  --color-error: #d13438;
  --color-warning: #ff8c00;
  --color-border: #d0d0d0;
  --color-border-light: #e0e0e0;
  --color-grid: #e8e8e8;
}
```

### 1.2 字体系统

#### 字体家族

- **主要字体**：思源宋体 (SourceHanSerifCN-Regular-1.otf)
- **辅助字体**：得意黑 (SmileySans-Oblique.ttf)
- **等宽字体**：Consolas

#### 字体大小

|组件类型 |字体大小 |字体家族 |字重 |样式 |
|---|---|---|---|---|
|主标题 |24px |得意黑 |加粗 |- |
|副标题 |12px |得意黑 |常规 |- |
|对话框主标题 |16px |得意黑 |加粗 |- |
|对话框副标题 |11px |思源宋体 |常规 |- |
|协议窗口标题 |14px |得意黑 |加粗 |- |
|版本信息 |10px |得意黑 |加粗 |- |
|标语 |12px |思源宋体 |常规 |斜体 |
|按钮 |11px |思源宋体 |常规 |- |
|小按钮 |9px |思源宋体 |常规 |- |
|输入框 |11px |思源宋体 |常规 |- |
|下拉框 |11px |思源宋体 |常规 |- |
|表格内容 |11px |思源宋体 |常规 |- |
|表格标题 |10px |思源宋体 |加粗 |- |
|分组框标题 |12px |思源宋体 |加粗 |- |
|标签 |12px |思源宋体 |常规 |- |
|状态栏 |10px |思源宋体 |常规 |- |
|状态栏快捷键 |9px |思源宋体 |常规 |- |
|日志 |10px |等宽字体 |常规 |- |
|脚本 |12px |等宽字体 |常规 |- |
|协议标题 |20px |思源宋体 |加粗 |- |
|协议二级标题 |16px |思源宋体 |加粗 |- |
|协议三级标题 |14px |思源宋体 |加粗 |- |
|协议正文 |12px |思源宋体 |常规 |- |
|协议次要文本 |11px |思源宋体 |常规 |- |
|版权信息 |8px |思源宋体 |常规 |- |
|关于页面文本框 |12px |思源宋体 |常规 |- |

```css
:root {
  --font-size-xxsmall: 8px;
  --font-size-xsmall: 9px;
  --font-size-small: 10px;
  --font-size-base: 11px;
  --font-size-medium: 12px;
  --font-size-large: 14px;
  --font-size-xlarge: 16px;
  --font-size-xxlarge: 24px;
  --font-family-main: "SourceHanSerifCN", serif;
  --font-family-secondary: "SmileySans", sans-serif;
  --font-family-mono: "Consolas", monospace;
}
```

### 1.3 圆角

所有UI组件圆角统一遵循WinUI3设计规范，使用8px圆角以确保视觉风格一致性和现代化。

|组件类型 |圆角大小 |
|---|---|
|按钮 |8px |
|输入框 |8px |
|下拉框 |8px |
|数字输入框 |8px |
|文本框 |8px |
|表格 |8px |
|分组框 |8px |
|菜单栏 |8px |
|菜单项 |8px |
|下拉菜单 |8px |
|搜索容器 |8px |
|滚动条 |5px |
|分割器 |3px |

```css
:root {
  --border-radius-small: 4px;
  --border-radius-base: 8px;  /* WinUI3标准圆角 */
  --border-radius-medium: 8px;
}
```

### 1.4 阴影

由于PyQt不支持CSS box-shadow属性，实际实现中阴影效果已被移除。

```css
:root {
  --shadow-small: '';
  --shadow-medium: '';
  --shadow-large: '';
}
```

### 1.5 间距

所有按钮与输入框、下拉框的高度统一为32px（min-height: 20px + padding: 6px上 + 6px下），确保同一行内的组件视觉对齐。

|组件类型 |内边距 |高度 |外边距 |
|---|---|---|---|
|按钮 |6px 12px |32px |- |
|输入框 |6px 8px |32px |- |
|下拉框 |6px 8px |32px |- |
|数字输入框 |6px 4px |32px |- |
|表格项 |6px 8px |- |- |
|分组框 |12px (顶部) |- |12px (顶部) |
|分组框标题 |0 8px |- |- |
|标签 |- |- |0 15px |

```css
:root {
  --padding-small: 4px;
  --padding-base: 6px 8px;
  --padding-medium: 6px 12px;  /* 按钮内边距 */
  --padding-large: 10px;
  --component-height: 32px;  /* 统一组件高度 */
  --component-min-height: 20px;  /* 最小高度 */
}
```

### 1.6 过渡动画

|动画类型 |持续时间 |缓动函数 |描述 |
|---|---|---|---|
|窗口淡入 |300ms |ease-out |窗口显示时的淡入效果 |
|窗口淡出 |300ms |ease-in |窗口隐藏时的淡出效果 |
|弹窗淡入 |200ms |ease-out |弹窗显示时的淡入效果 |
|弹窗淡出 |200ms |ease-in |弹窗隐藏时的淡出效果 |
|弹窗缩放 |200ms |ease-out |弹窗显示时的缩放效果 |

```css
:root {
  --transition-duration-short: 200ms;
  --transition-duration-base: 300ms;
  --transition-ease-in: ease-in;
  --transition-ease-out: ease-out;
  --transition-ease-in-out: ease-in-out;
}
```

## 2. UI组件样式

### 2.0 消息框

#### 消息框基础样式

```css
.dialog-message-box {
  background-color: white;
  border-radius: 8px;
  padding: 15px;
  min-width: 200px;
  max-width: 400px;
  text-align: center;
}

.dialog-message-content {
  font-family: var(--font-family-main);
  font-size: 13px;
  color: var(--color-text);
  margin-bottom: 15px;
  text-align: center;
}

.dialog-button-container {
  display: flex;
  justify-content: center;
  gap: 10px;
}
```

#### 消息框类型

|消息框类型 |描述 |按钮配置 |
|---|---|---|
|警告消息框 |显示警告信息 |单按钮（确定），居中显示 |
|错误消息框 |显示错误信息 |单按钮（确定），居中显示 |
|信息消息框 |显示普通信息 |单按钮（确定），居中显示 |
|询问消息框 |显示询问信息 |双按钮（是/否），居中显示 |

### 2.0.1 对话框按钮布局规范

所有对话框下方的「确认」「关闭」「确定」「取消」等按钮需要**居中显示**，确保视觉平衡和整体美观。

**布局要求：**

- 对话框按钮区域使用水平盒模型布局（如 PyQt 中的 `QHBoxLayout`）
- 按钮前后均添加弹性空间（`addStretch()`），实现居中对齐
- 多按钮场景时，按钮间距保持一致（通常 10px）
- 按钮总宽度不超过对话框内容宽度

**实现示例（PyQt6）：**

```python
button_layout = QHBoxLayout()
button_layout.addStretch()  # 左侧弹性空间
button_layout.addWidget(ok_button)
button_layout.addWidget(cancel_button)
button_layout.addStretch()  # 右侧弹性空间
```

**CSS 参考：**

```css
.dialog-button-container {
  display: flex;
  justify-content: center;  /* 居中对齐 */
  gap: 10px;  /* 按钮间距 */
  margin-top: 15px;  /* 与内容的间距 */
}
```

### 2.1 按钮

#### 主按钮

```css
.primary-button {
  background-color: var(--color-primary);
  color: white;
  border: none;
  border-radius: 8px;  /* WinUI3标准圆角 */
  padding: 6px 12px;
  font-weight: bold;
  font-size: var(--font-size-base);
  min-height: 20px;
  max-height: 20px;
  box-shadow: var(--shadow-small);
}

.primary-button:hover {
  background-color: var(--color-primary-hover);
}

.primary-button:active {
  background-color: var(--color-primary-pressed);
}
```

#### 次要按钮

```css
.secondary-button {
  background-color: var(--color-secondary);
  color: var(--color-text);
  border: 1px solid var(--color-border);
  border-radius: 8px;  /* WinUI3标准圆角 */
  padding: 6px 12px;
  font-size: var(--font-size-base);
  min-height: 20px;
  max-height: 20px;
  box-shadow: var(--shadow-small);
}

.secondary-button:hover {
  background-color: var(--color-secondary-hover);
}

.secondary-button:active {
  background-color: var(--color-secondary-pressed);
}
```

#### 禁用按钮

```css
.disabled-button {
  background-color: #f5f5f5;
  color: #999999;
  border: 1px solid var(--color-border);
  border-radius: 8px;  /* WinUI3标准圆角 */
  padding: 6px 12px;
  font-size: var(--font-size-base);
  min-height: 20px;
  max-height: 20px;
  box-shadow: var(--shadow-small);
}
```

### 2.2 输入框

```css
.input {
  border: 1px solid var(--color-border);
  border-radius: 8px;  /* WinUI3标准圆角 */
  padding: 6px 8px;
  background-color: white;
  font-size: var(--font-size-base);
  selection-background-color: var(--color-primary);
  box-shadow: var(--shadow-small);
  min-height: 20px;
  max-height: 20px;
}

.input:focus {
  border-color: var(--color-primary);
  background-color: #fafafa;
}

.input:hover {
  border-color: #a0a0a0;
}
```

### 2.3 下拉框

```css
.select {
  border: 1px solid var(--color-border);
  border-radius: 8px;  /* WinUI3标准圆角 */
  padding: 6px 8px;
  background-color: white;
  font-size: var(--font-size-base);
  min-width: 80px;
  box-shadow: var(--shadow-small);
  min-height: 20px;
  max-height: 20px;
}

.select:hover {
  border-color: #a0a0a0;
}

.select:focus {
  border-color: var(--color-primary);
}

.select-dropdown {
  border: 1px solid var(--color-border);
  border-radius: 8px;  /* WinUI3标准圆角 */
  background-color: white;
  selection-background-color: var(--color-primary);
  selection-color: white;
  font-size: var(--font-size-base);
  padding: 4px;
  box-shadow: var(--shadow-small);
}
```

### 2.4 表格

```css
.table {
  border: 1px solid var(--color-border);
  border-radius: 8px;  /* WinUI3标准圆角 */
  background-color: white;
  gridline-color: #f0f0f0;
  font-size: var(--font-size-base);
  outline: none;
  box-shadow: var(--shadow-medium);
}

.table-cell {
  padding: 6px 8px;
  border: none;
  text-align: center;
}

.table-cell:hover {
  background-color: #CAE9FF;
}

.table-cell-selected {
  background-color: var(--color-primary);
  color: white;
}

.table-header {
  background-color: #ffffff;
  padding: 4px 10px;
  border: none;
  border-right: 1px solid var(--color-border-light);
  border-bottom: 1px solid var(--color-border-light);
  font-weight: bold;
  font-size: var(--font-size-small);
  color: #333333;
  text-align: center;
  min-height: 20px;
}

.table-header:last {
  border-right: none;
}

.table-corner {
  background-color: #ffffff;
  border: none;
  border-right: 1px solid var(--color-border-light);
  border-bottom: 1px solid var(--color-border-light);
}
```

### 2.5 分组框

#### 主窗口分组框

```css
.group-box {
  font-size: var(--font-size-medium);
  font-weight: bold;
  color: var(--color-primary);
  border: 1px solid var(--color-border-light);
  border-radius: 8px;  /* WinUI3标准圆角 */
  margin-top: 12px;
  padding-top: 12px;
  box-shadow: var(--shadow-medium);
}

.group-box-title {
  subcontrol-origin: margin;
  subcontrol-position: top center;
  padding: 0 8px 0 8px;
}
```

#### 对话框分组框

对话框中的分组框样式与主窗口一致，但标题背景色需设置为白色（#ffffff）以实现边框断开效果，确保标题位置的边框被白色背景"挖空"。

```css
.dialog .group-box {
  font-size: var(--font-size-medium);
  font-weight: bold;
  color: var(--color-primary);
  border: 1px solid var(--color-border-light);
  border-radius: 8px;  /* WinUI3标准圆角 */
  margin-top: 12px;
  padding-top: 12px;
  /* 不设置background-color，保持继承 */
}

.dialog .group-box-title {
  subcontrol-origin: margin;
  subcontrol-position: top center;
  padding: 0 8px 0 8px;
  background-color: #ffffff;  /* 关键：白色背景遮挡边框 */
}
```

**设计说明：**
- 标题左右各留8px的白色背景区域，使边框线在标题位置形成断开效果
- 标题颜色为主题蓝色（#66ccff），加粗显示，视觉层次清晰
- 边框颜色与主窗口保持一致（浅灰色#e0e0e0），确保整体UI统一

### 2.6 滚动条

```css
/* 垂直滚动条 */
.scrollbar-vertical {
  background-color: transparent;
  width: 10px;
  margin: 10px 0;
}

.scrollbar-vertical .groove {
  background-color: rgba(0, 0, 0, 0.08);
  border-radius: var(--border-radius-base);
  margin: 0 4px;
}

.scrollbar-vertical .handle {
  background-color: var(--color-border);
  border-radius: var(--border-radius-base);
  min-height: 20px;
  margin: 0 4px;
}

.scrollbar-vertical .handle:hover {
  background-color: var(--color-primary-hover);
}

.scrollbar-vertical .handle:active {
  background-color: var(--color-primary-pressed);
}

/* 水平滚动条 */
.scrollbar-horizontal {
  background-color: transparent;
  height: 10px;
  margin: 0 10px;
}

.scrollbar-horizontal .groove {
  background-color: rgba(0, 0, 0, 0.08);
  border-radius: var(--border-radius-base);
  margin: 4px 0;
}

.scrollbar-horizontal .handle {
  background-color: var(--color-border);
  border-radius: var(--border-radius-base);
  min-width: 20px;
  margin: 4px 0;
}

.scrollbar-horizontal .handle:hover {
  background-color: var(--color-primary-hover);
}

.scrollbar-horizontal .handle:active {
  background-color: var(--color-primary-pressed);
}

/* 滚动区域 */
.scroll-area {
  border: none;
  background-color: transparent;
}

.scroll-area > QWidget > QWidget {
  background-color: transparent;
}

/* 滚动条容器 */
.scroll-container {
  background-color: transparent;
}
```

### 2.7 文本编辑器

```css
.text-editor {
  font-family: var(--font-family-mono), var(--font-family-main);
  font-size: var(--font-size-base);
  background-color: #ffffff;
  border: 1px solid var(--color-border-light);
  border-radius: 8px;  /* WinUI3标准圆角 */
  padding: var(--padding-large);
}
```

### 2.8 日志显示

```css
.log-display {
  font-family: var(--font-family-mono), var(--font-family-main);
  font-size: var(--font-size-small);
  background-color: #1e1e1e;
  color: #d4d4d4;
  border: 1px solid var(--color-border);
  border-radius: 8px;  /* WinUI3标准圆角 */
  padding: var(--padding-large);
}
```

### 2.9 文本浏览器

```css
.text-browser {
  background-color: #f8f9fa;
  border: 1px solid var(--color-border-light);
  border-radius: 8px;  /* WinUI3标准圆角 */
  padding: var(--padding-large);
  font-size: var(--font-size-small);
  line-height: 1.3;
  box-shadow: var(--shadow-small);
}

.text-browser a {
  color: var(--color-primary);
  text-decoration: none;
}

.text-browser a:hover {
  color: var(--color-primary-hover);
  text-decoration: underline;
}
```

### 2.9.1 用户协议文本浏览器

用户协议文本浏览器用于显示用户协议HTML内容，与程序设计语言保持完全一致。

```css
/* 用户协议HTML内嵌样式 */
.user-agreement-html {
  font-family: "Source Han Serif CN", "思源宋体", "Microsoft YaHei", "SimSun", serif;
  font-size: 12px;
  line-height: 1.8;
  background-color: #ffffff;
  color: var(--color-text);
}

.user-agreement-html h1 {
  font-family: "Source Han Serif CN", "思源宋体", "Microsoft YaHei", "SimSun", serif;
  font-size: 20px;
  font-weight: 600;
  color: var(--color-primary);
}

.user-agreement-html h2 {
  font-family: "Source Han Serif CN", "思源宋体", "Microsoft YaHei", "SimSun", serif;
  font-size: 16px;
  font-weight: 600;
  color: var(--color-primary);
  border-left: 4px solid var(--color-primary);
  padding-left: 0.8em;
  margin-top: 1.5em;
  margin-bottom: 1em;
}

.user-agreement-html h3 {
  font-family: "Source Han Serif CN", "思源宋体", "Microsoft YaHei", "SimSun", serif;
  font-size: 14px;
  font-weight: 600;
  color: var(--color-text);
  margin-top: 1.2em;
  margin-bottom: 0.8em;
}

.user-agreement-html .last-modified {
  font-size: 11px;
  color: var(--color-text-secondary);
}

.user-agreement-html .warning {
  color: var(--color-error);
  font-weight: bold;
}

.user-agreement-html .important {
  color: var(--color-warning);
  font-weight: bold;
}

.user-agreement-html a {
  color: var(--color-primary);
  text-decoration: none;
}

.user-agreement-html a:hover {
  text-decoration: underline;
}
```

### 2.10 状态栏

```css
.status-bar {
  background-color: #ffffff;
  color: var(--color-text);
  border-top: 1px solid var(--color-border);
  font-size: var(--font-size-small);
}
```

### 2.11 分割器

```css
.splitter {
  background-color: transparent;
  width: 6px;
  height: 6px;
  border-radius: var(--border-radius-small);
}

.splitter:hover {
  background-color: transparent;
}
```

### 2.12 搜索容器

```css
.search-container {
  background-color: #ffffff;
  border: none;
  border-radius: var(--border-radius-medium);
  box-shadow: var(--shadow-small);
}
```

### 2.13 搜索输入框

```css
.search-input {
  border: 1px solid var(--color-border);
  border-radius: 8px;  /* WinUI3标准圆角 */
  padding: 6px 8px;
  background-color: white;
  font-size: var(--font-size-base);
  selection-background-color: var(--color-primary);
  box-shadow: var(--shadow-small);
  min-height: 30px;
  max-height: 30px;
}

.search-input:focus {
  border-color: var(--color-primary);
  background-color: #fafafa;
}

.search-input:hover {
  border-color: #a0a0a0;
}
```

### 2.14 过滤组合框

```css
.filter-select {
  border: 1px solid var(--color-border);
  border-radius: 8px;  /* WinUI3标准圆角 */
  padding: 6px 8px;
  background-color: white;
  font-size: var(--font-size-base);
  min-width: 80px;
  box-shadow: var(--shadow-small);
  min-height: 30px;
  max-height: 30px;
}

.filter-select:hover {
  border-color: #a0a0a0;
}

.filter-select:focus {
  border-color: var(--color-primary);
}
```

### 2.15 整数/浮点数输入框

```css
.number-input {
  border: 1px solid var(--color-border);
  border-radius: 8px;  /* WinUI3标准圆角 */
  padding: 6px 4px;
  background-color: white;
  font-size: var(--font-size-base);
  selection-background-color: var(--color-primary);
  text-align: center;
  box-shadow: var(--shadow-small);
  min-height: 20px;
  max-height: 20px;
}

.number-input:focus {
  border-color: var(--color-primary);
  background-color: #fafafa;
  border-width: 1.5px;
}

.number-input:hover {
  border-color: #a0a0a0;
  background-color: #fafafa;
}
```

### 2.16 复选框

```css
.checkbox {
  color: var(--color-text);
  spacing: 6px;
}

.checkbox::indicator {
  width: 18px;
  height: 18px;
}
```

### 2.17 事件对话框

```css
.event-dialog {
  background-color: var(--color-bg);
}
```

### 2.18 绝对时间偏移显示框

```css
.absolute-time-edit {
  border: 1px solid var(--color-border);
  border-radius: 8px;  /* WinUI3标准圆角 */
  padding: 6px 8px;
  background-color: var(--color-bg);
  font-size: 11px;
  selection-background-color: var(--color-primary);
  box-shadow: var(--shadow-small);
  min-height: 20px;
  max-height: 20px;
}
```

### 2.19 现代化控件类

#### 现代化分组框

现代化分组框与标准分组框样式一致。

```css
.modern-group-box {
  font-size: var(--font-size-medium);
  font-weight: bold;
  color: var(--color-primary);
  border: 1px solid var(--color-border-light);
  border-radius: 8px;  /* WinUI3标准圆角 */
  margin-top: 12px;
  padding-top: 12px;
  box-shadow: var(--shadow-medium);
}

.modern-group-box-title {
  subcontrol-origin: margin;
  subcontrol-position: top center;
  padding: 0 8px 0 8px;
}
```

**对话框中的现代化分组框：**

对话框中的现代化分组框需设置标题背景色为白色（#ffffff）以实现边框断开效果。

```css
.dialog .modern-group-box {
  font-size: var(--font-size-medium);
  font-weight: bold;
  color: var(--color-primary);
  border: 1px solid var(--color-border-light);
  border-radius: 8px;  /* WinUI3标准圆角 */
  margin-top: 12px;
  padding-top: 12px;
  /* 不设置background-color，保持继承 */
}

.dialog .modern-group-box-title {
  subcontrol-origin: margin;
  subcontrol-position: top center;
  padding: 0 8px 0 8px;
  background-color: #ffffff;  /* 关键：白色背景遮挡边框 */
}
```

#### 现代化输入框

```css
.modern-line-edit {
  border: 1px solid var(--color-border);
  border-radius: 8px;  /* WinUI3标准圆角 */
  padding: 6px 8px;
  background-color: white;
  font-size: var(--font-size-base);
  selection-background-color: var(--color-primary);
  box-shadow: var(--shadow-small);
  min-height: 20px;
  max-height: 20px;
  text-align: center;
}

.modern-line-edit:focus {
  border-color: var(--color-primary);
  background-color: #fafafa;
}

.modern-line-edit:hover {
  border-color: #a0a0a0;
}
```

#### 现代化下拉框

```css
.modern-combo-box {
  border: 1px solid var(--color-border);
  border-radius: 8px;  /* WinUI3标准圆角 */
  padding: 6px 8px;
  background-color: white;
  font-size: var(--font-size-base);
  min-width: 80px;
  box-shadow: var(--shadow-small);
  min-height: 20px;
  max-height: 20px;
  text-align: center;
}

.modern-combo-box:hover {
  border-color: #a0a0a0;
}

.modern-combo-box:focus {
  border-color: var(--color-primary);
}

.modern-combo-box-dropdown {
  border: 1px solid var(--color-border);
  border-radius: 8px;  /* WinUI3标准圆角 */
  background-color: white;
  selection-background-color: var(--color-primary);
  selection-color: white;
  font-size: var(--font-size-base);
  padding: 4px;
  box-shadow: var(--shadow-small);
}
```

#### 现代化整数输入框

```css
.modern-spin-box {
  border: 1px solid var(--color-border);
  border-radius: 8px;  /* WinUI3标准圆角 */
  padding: 6px 4px 6px 4px;
  background-color: white;
  font-size: var(--font-size-base);
  selection-background-color: var(--color-primary);
  text-align: center;
  box-shadow: var(--shadow-small);
  min-height: 20px;
  max-height: 20px;
}

.modern-spin-box:focus {
  border-color: var(--color-primary);
  background-color: #fafafa;
  border-width: 1.5px;
}

.modern-spin-box:hover {
  border-color: #a0a0a0;
  background-color: #fafafa;
}
```

#### 现代化浮点数输入框

```css
.modern-double-spin-box {
  border: 1px solid var(--color-border);
  border-radius: 8px;  /* WinUI3标准圆角 */
  padding: 6px 4px 6px 4px;
  background-color: white;
  font-size: var(--font-size-base);
  selection-background-color: var(--color-primary);
  text-align: center;
  box-shadow: var(--shadow-small);
  min-height: 20px;
  max-height: 20px;
}

.modern-double-spin-box:focus {
  border-color: var(--color-primary);
  background-color: #fafafa;
  border-width: 1.5px;
}

.modern-double-spin-box:hover {
  border-color: #a0a0a0;
  background-color: #fafafa;
}
```

#### 居中组合框

```css
.centered-combo-box {
  background-color: var(--color-card-bg);
  color: var(--color-text);
  border: 1px solid var(--color-border);
  border-radius: 8px;  /* WinUI3标准圆角 */
  padding: 6px 8px;
  font-size: 11px;
  text-align: center;
  min-height: 20px;
  max-height: 20px;
}

.centered-combo-box::drop-down {
  border: none;
  width: 0px;
}

.centered-combo-box::down-arrow {
  image: none;
}
```

#### 居中输入框

```css
.centered-line-edit {
  border: 1px solid var(--color-border);
  border-radius: 8px;  /* WinUI3标准圆角 */
  padding: 6px 8px;
  background-color: white;
  font-size: var(--font-size-base);
  selection-background-color: var(--color-primary);
  box-shadow: var(--shadow-small);
  min-height: 20px;
  max-height: 20px;
  text-align: center;
}

.centered-line-edit:focus {
  border-color: var(--color-primary);
  background-color: #fafafa;
}

.centered-line-edit:hover {
  border-color: #a0a0a0;
}
```

#### 时间偏移输入框

```css
.time-offset-spin-box {
  border: 1px solid var(--color-border);
  border-radius: 8px;  /* WinUI3标准圆角 */
  padding: 6px 8px;
  background-color: white;
  font-family: "SourceHanSerifCN";
  font-size: 11px;
  selection-background-color: var(--color-primary);
  text-align: center;
  box-shadow: var(--shadow-small);
  min-height: 20px;
  max-height: 20px;
}

.time-offset-spin-box:focus {
  border-color: var(--color-primary);
  background-color: #fafafa;
  border-width: 1.5px;
}

.time-offset-spin-box:hover {
  border-color: #a0a0a0;
  background-color: #fafafa;
}
```

#### 说明文本编辑器

```css
.explanation-text-edit {
  background-color: #ffffff;
  border: 1px solid var(--color-border-light);
  border-radius: 6px;
  padding: 6px;
  font-family: "SourceHanSerifCN";
  font-size: 10px;
  box-shadow: var(--shadow-small);
}
```

#### 居中组合框列表视图

```css
.centered-combobox-listview {
  background-color: var(--color-card-bg);
  color: var(--color-text);
  font-family: "SourceHanSerifCN";
  font-size: 11px;
  outline: none;
  show-decoration-selected: 0;
}

.centered-combobox-listview::item {
  padding: 8px;
  text-align: center;
  border: none;
}

.centered-combobox-listview::item:selected {
  background-color: var(--color-primary);
  color: white;
}
```

#### 现代化菜单

```css
.modern-menu {
  background-color: var(--color-bg);
  border: 1px solid var(--color-border);
  border-radius: 6px;
  padding: 6px;
  box-shadow: var(--shadow-small);
}

.modern-menu-item {
  padding: 6px 16px;
  border-radius: 4px;
  margin: 2px 2px;
}

.modern-menu-item:hover {
  background-color: var(--color-primary-hover);
  color: white;
}

.modern-menu-separator {
  height: 1px;
  background-color: var(--color-border-light);
  margin: 4px 8px;
}
```

## 3. 布局组件

### 3.1 容器

```css
.container {
  background-color: var(--color-bg);
}
```

### 3.2 卡片

```css
.card {
  background-color: var(--color-card-bg);
  border: 1px solid var(--color-border-light);
  border-radius: var(--border-radius-base);
  padding: var(--padding-large);
  box-shadow: var(--shadow-small);
}
```

## 4. 状态样式

### 4.1 捕获状态

```css
.capture-status {
  color: var(--color-text-secondary);
  font-size: var(--font-size-small);
}

.capture-status-active {
  color: var(--color-primary);
}

.capture-status-inactive {
  color: var(--color-text-secondary);
}

.capture-status-bold {
  color: var(--color-primary);
  font-weight: bold;
}
```

### 4.2 绝对时间信息

```css
.absolute-time {
  color: var(--color-text-secondary);
  font-size: 9px;
}
```

## 5. 使用示例

### 5.1 按钮示例

```html
<button class="primary-button">主按钮</button>
<button class="secondary-button">次要按钮</button>
<button class="disabled-button" disabled>禁用按钮</button>
```

### 5.2 表单示例

```html
<form>
  <div class="form-group">
    <label for="input1">输入框：</label>
    <input type="text" id="input1" class="input" placeholder="请输入内容">
  </div>
  <div class="form-group">
    <label for="select1">下拉框：</label>
    <select id="select1" class="select">
      <option value="1">选项1</option>
      <option value="2">选项2</option>
      <option value="3">选项3</option>
    </select>
  </div>
  <div class="form-group">
    <button type="submit" class="primary-button">提交</button>
    <button type="reset" class="secondary-button">重置</button>
  </div>
</form>
```

### 5.3 表格示例

```html
<table class="table">
  <thead>
    <tr>
      <th class="table-header">列1</th>
      <th class="table-header">列2</th>
      <th class="table-header">列3</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td class="table-cell">内容1</td>
      <td class="table-cell">内容2</td>
      <td class="table-cell">内容3</td>
    </tr>
    <tr>
      <td class="table-cell">内容4</td>
      <td class="table-cell">内容5</td>
      <td class="table-cell">内容6</td>
    </tr>
  </tbody>
</table>
```

## 6. 全局样式

```css
/* 全局样式重置 */
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  font-family: var(--font-family-main);
  font-size: var(--font-size-base);
  color: var(--color-text);
  background-color: var(--color-bg);
}

/* 主窗口样式 */
.main-window {
  background-color: var(--color-bg);
  transition: opacity var(--transition-duration-base) var(--transition-ease-in-out);
}

/* 对话框样式 */
.dialog {
  background-color: var(--color-bg);
  transition: opacity var(--transition-duration-short) var(--transition-ease-in-out),
              transform var(--transition-duration-short) var(--transition-ease-out);
}

/* 窗口过渡动画 */
.window-enter {
  opacity: 0;
}

.window-enter-active {
  opacity: 1;
  transition: opacity var(--transition-duration-base) var(--transition-ease-out);
}

.window-exit {
  opacity: 1;
}

.window-exit-active {
  opacity: 0;
  transition: opacity var(--transition-duration-base) var(--transition-ease-in);
}

/* 弹窗过渡动画 */
.popup-enter {
  opacity: 0;
  transform: scale(0.95);
}

.popup-enter-active {
  opacity: 1;
  transform: scale(1);
  transition: opacity var(--transition-duration-short) var(--transition-ease-out),
              transform var(--transition-duration-short) var(--transition-ease-out);
}

.popup-exit {
  opacity: 1;
  transform: scale(1);
}

.popup-exit-active {
  opacity: 0;
  transform: scale(0.95);
  transition: opacity var(--transition-duration-short) var(--transition-ease-in),
              transform var(--transition-duration-short) var(--transition-ease-in);
}

/* 分组框全局样式 */
.group-box {
  background-color: var(--color-bg);
}

/* 对话框分组框样式 */
.dialog .group-box {
  border: 1px solid var(--color-border-light);
  border-radius: 8px;
  margin-top: 12px;
  padding-top: 12px;
}

.dialog .group-box::title {
  subcontrol-origin: margin;
  subcontrol-position: top center;
  padding: 0 8px 0 8px;
  background-color: #ffffff;  /* 关键：白色背景遮挡边框，实现边框断开效果 */
}

/* 菜单栏样式 */
.menu-bar {
  background-color: var(--color-bg);
  border: none;
  padding: 4px;
}

.menu-bar-item {
  padding: 4px 8px;
  border-radius: var(--border-radius-small);
}

.menu-bar-item:hover {
  background-color: var(--color-primary-hover);
  color: white;
}

/* 菜单样式 */
.menu {
  background-color: var(--color-bg);
  border: 1px solid var(--color-border);
  border-radius: var(--border-radius-small);
  padding: 4px;
  box-shadow: var(--shadow-small);
}

.menu-item {
  padding: 4px 16px;
  border-radius: var(--border-radius-small);
}

.menu-item:hover {
  background-color: var(--color-primary-hover);
  color: white;
}

/* 菜单项样式 */
.menu-action:hover {
  background-color: var(--color-primary-hover);
  color: white;
}
```

## 7. 设计原则


1. **简洁性**：采用扁平化设计，减少视觉干扰
2. **一致性**：所有组件使用统一的颜色、字体和间距
3. **可用性**：清晰的视觉层次和反馈机制
4. **可访问性**：确保文本与背景的对比度符合标准
5. **响应式**：适配不同屏幕尺寸

## 8. 扩展建议


1. 考虑添加暗色主题支持
2. 增加更多的状态反馈样式
3. 考虑添加动画过渡效果
4. 优化移动端适配

## 9. 版本历史

|版本 |日期 |变更内容 |
|---|---|---|
|2.2 |2025-12-25 |1. 完善分组框样式规范，区分主窗口和对话框分组框；2. 对话框分组框标题背景色设为白色（#ffffff）以实现边框断开效果；3. 添加现代化分组框的对话框样式规范；4. 在全局样式中添加对话框分组框样式规则 |
|2.1 |2025-12-20 |1. 新增对话框按钮布局规范（2.0.1）；2. 规范所有对话框下方按钮需居中显示；3. 提供PyQt6实现示例和CSS参考；4. 更新消息框类型配置表注明按钮居中显示 |
|2.0 |2025-12-20 |1. 所有UI组件圆角统一为8px，遵循WinUI3设计规范；2. 按钮高度与输入框、下拉框统一为32px；3. 按钮内边距从8px 12px调整为6px 12px；4. 菜单栏、菜单项圆角统一为8px；5. 更新所有现代化控件类圆角规范 |
|1.9 |2025-12-18 |1. 添加现代化菜单样式；2. 新增复选框样式；3. 新增事件对话框样式；4. 新增绝对时间偏移显示框样式；5. 更新现代化控件类序号 |
|1.8 |2025-12-17 |1. 更新滚动条样式为Fluent Design风格；2. 调整滚动条宽度和高度为10px；3. 滚动条圆角从4px改为5px；4. 添加滚动区域和滚动条容器样式 |
|1.7 |2025-12-17 |1. 更新关于页面文本框字体大小从10px到12px；2. 调整文档结构，确保与产品版本3.8.2一致 |
|1.6 |2025-12-17 |1. 添加窗口和弹窗的过渡动画规范；2. 在基础元素中增加过渡动画定义；3. 在全局样式中添加窗口和弹窗的过渡动画效果 |
|1.5 |2025-12-15 |1. 添加用户协议HTML样式规范；2. 统一协议字体为思源宋体；3. 统一协议颜色系统与程序主色调一致；4. 添加协议各级标题字体大小规范 |
|1.4 |2025-12-15 |1. 修改消息框为响应式布局，移除固定宽度限制；2. 设置最小宽度200px和最大宽度400px；3. 确保长文本内容能正常显示；4. 保持按钮水平居中布局 |
|1.3 |2025-12-15 |1. 添加消息框样式规范；2. 调整消息框宽度为150px；3. 规范消息框按钮布局为水平居中；4. 统一消息框样式和字体 |
|1.2 |2025-12-13 |1. 更新主色悬停颜色从#66ccff为#4db8ff；2. 添加现代化控件类样式；3. 更新阴影效果为PyQt兼容版本 |
|1.1 |2025-12-10 |更新表格样式规范，调整字体大小、行高和边框设置 |
|1.0 |2025-12-10 |初始版本，提取项目现有设计语言 |


